name: Status check functions
on: push
jobs:
  fail:
    runs-on: ubuntu-latest
    steps:
      - run: exit 1 # 終了ステータスが0以外なので、エラーが発生する
      - run: echo "Hello" # エラーが発生しても、failure関数があるので実行される
        if: ${{ failure() }}
